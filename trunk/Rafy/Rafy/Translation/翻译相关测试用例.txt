多国语言测试用例：
    元数据
        列头
        命令
            分组后的命令
            Tooltip
        模块名
            模块页签名
            模块列表名
        表单中编辑器中的 Label
        子页签的名称
    弹出框。
    枚举Label
    弹出窗口
    权限系统中使用的分组名称
    选择列的名称
    xaml中的汉字

另外：
如果是非框架提供的字符串输出、或者是动态拼接字符串输出的情况下，则需要手工调用 Translate 方法来进行翻译。
问题：这些动态的字符串，不何让框架不去收集它们？

框架内部实现时注意：
翻译工作尽量在最后、最接近界面生成时执行，这样可以保证在调试代码时，可以看到中文，并节省不必要的翻译工作。