<UserControl x:Class="Rafy.DevTools.Theme.ThemeDesigner"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="522"
        d:DesignWidth="783">
    <DockPanel Margin="4">
        <ToolBar VerticalAlignment="Top" DockPanel.Dock="Top">
            <TextBox x:Name="txtPath" Text="填写需要加载的 xaml 文件路径" Width="550" Margin="0" />
            <Button x:Name="btnRefresh" Content="绑定" Click="btnRefresh_Click" Padding="10,2" />
        </ToolBar>
        <TextBox x:Name="txtContent" TextWrapping="Wrap" Margin="0,4,0,0" HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto" AcceptsReturn="True" IsReadOnly="True" Visibility="Hidden" />
    </DockPanel>
</UserControl>
