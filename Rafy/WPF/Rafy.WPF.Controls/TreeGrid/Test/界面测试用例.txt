选择行
添加、删除、插入子
上下移、升降级
排序、过滤、分组
编辑
    单元格编辑
        * 按 tab 键开始编辑下一个单元格。
    行编辑
虚拟化
    行虚拟化
        * 表格状态下，10000 行数据显示后可视树中的行，实例化的行数不超过两屏。
        * 向下滚动滚动条时，行数不超过两屏。
    列虚拟化
        * 表格状态下，200 列数据显示后的可视树中的单元格，每行中实例化的单元格不应该超过一屏。
动态列宽
    * 当前界面的列头及数据都应该影响列宽。
    * 当数据长度大于 400 时，只显示 400。
    * 双击列头，重新计算动态列宽，并不受最大动态宽度的影响。
其它功能